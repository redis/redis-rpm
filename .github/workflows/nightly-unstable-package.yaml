name: Nightly Unstable Build and Package

on:
  schedule:
    - cron: '30 13 * * *'  # Run at 1:00 AM UTC every day
  workflow_dispatch:  # Allow manual triggering

jobs:
  call-unstable-build:
    uses: redislabsdev/redis-rpm/.github/workflows/rpm.yml@unstable
